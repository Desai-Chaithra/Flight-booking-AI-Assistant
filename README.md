***NOTE***
DEAR USERS,\n\n
    if you're following this code, then make sure to do these steps to get everything right.
    1. Create an API key in platform.openai.com
    2. Copy the API key and create a .env file in project folder.
    3. In the .env file, paste the API key as OPENAI_API_KEY = XXXX (paste your key here)
    4. Follow these steps to connect with the OpenAI from your project.
    5. After adding the API key, don't forget to save the .env file.
    
